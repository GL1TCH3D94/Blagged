def blagged

stage('Clone Repository')
{
	checkout scm
}
stage('Build')
{
	blagged = docker.build()
}
stage('Push image')
{
	docker.withRegistry('https://registry.hub.docker.com', 'docker-hub-credentials')
	{
		app.push("${env.BUILD_NUMBER}")
		app.push("latest")
	}
}
